# Service Kubernetes pour le pod Profiles
# Expose le port 3002
apiVersion: v1
kind: Service
metadata:
  name: profiles
  namespace: chat-app
spec:
  selector:
    app: profiles
  ports:
    - port: 3002
      targetPort: 3002
