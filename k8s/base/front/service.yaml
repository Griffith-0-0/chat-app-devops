# Service Kubernetes pour le pod Front
# Expose le port 80 (Nginx)
apiVersion: v1
kind: Service
metadata:
  name: front
  namespace: chat-app
spec:
  selector:
    app: front
  ports:
    - port: 80
      targetPort: 80
