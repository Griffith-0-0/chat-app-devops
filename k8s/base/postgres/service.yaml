# Service headless pour PostgreSQL (clusterIP: None)
# Permet la résolution DNS stable pour le StatefulSet
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: chat-app
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
