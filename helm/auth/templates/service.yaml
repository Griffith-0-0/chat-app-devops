apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: chat-app
  labels:
    app: auth
spec:
  selector:
    app: auth
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}