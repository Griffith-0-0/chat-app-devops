apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: messaging-monitor
  namespace: chat-app
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: messaging
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
