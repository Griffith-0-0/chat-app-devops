apiVersion: v1
kind: Service
metadata:
  name: messaging
  namespace: chat-app
spec:
  selector:
    app: messaging
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
