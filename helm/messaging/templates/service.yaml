apiVersion: v1
kind: Service
metadata:
  name: messaging
  namespace: chat-app
  labels:
    app: messaging
spec:
  selector:
    app: messaging
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
