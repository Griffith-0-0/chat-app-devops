apiVersion: v1
kind: Service
metadata:
  name: profiles
  namespace: chat-app
spec:
  selector:
    app: profiles
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
