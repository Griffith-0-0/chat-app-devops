apiVersion: v1
kind: Service
metadata:
  name: profiles
  namespace: chat-app
  labels:
    app: profiles
spec:
  selector:
    app: profiles
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
