apiVersion: v1
kind: Service
metadata:
  name: front
  namespace: chat-app
spec:
  selector:
    app: front
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
